query GetFilteredProducts($productIds: [String!]) {
  products(options: { filter: { id: { in: $productIds } } }) {
    totalItems
    items {
      id
      name
      description
      featuredAsset {
        id
        preview
      }

      variants {
        id
        priceWithTax
      }
    }
  }
}
